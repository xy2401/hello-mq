# rocketmq/basic 完整实验流程：namesrv/broker/proxy → 拓扑初始化（mqadmin 由 run.sh 分阶段执行，
# setup 为 noop 保持 depends_on 编排对称）→ 生产 → 消费 → 幂等库检查。
# 独立运行：bash run.sh（Topic/消费组创建依赖 run.sh 内 mqadmin，直接 docker compose up 无法复现完整语义）
name: hello-mq-rocketmq-basic

x-java-base: &java-base
  image: ${TEMURIN_JRE_IMAGE:?TEMURIN_JRE_IMAGE must be set via --env-file ../.env.versions}
  volumes:
    - ../target/hello-mq-rocketmq.jar:/app/demo.jar:ro
    - ../../shared/fixtures:/app/fixtures:ro

services:
  namesrv:
    image: ${ROCKETMQ_IMAGE:?ROCKETMQ_IMAGE must be set via --env-file ../.env.versions}
    command: ["nameserver"]
    environment:
      # 官方脚本默认 namesrv 1g 堆；实验机资源有限，显式调小（HEAP_OPTS 由 runserver.sh 读取）。
      HEAP_OPTS: "-Xms256m -Xmx256m -Xmn128m"
    healthcheck:
      test: ["CMD-SHELL", "timeout 3 bash -c '</dev/tcp/127.0.0.1/9876'"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s

  broker:
    image: ${ROCKETMQ_IMAGE:?ROCKETMQ_IMAGE must be set via --env-file ../.env.versions}
    command: ["broker", "-c", "/home/rocketmq/conf/broker.conf"]
    environment:
      HEAP_OPTS: "-Xms512m -Xmx512m -Xmn256m -XX:MaxDirectMemorySize=512m"
    volumes:
      - ../broker.conf:/home/rocketmq/conf/broker.conf:ro
    depends_on:
      namesrv:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "timeout 3 bash -c '</dev/tcp/127.0.0.1/10911'"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 15s

  proxy:
    image: ${ROCKETMQ_IMAGE:?ROCKETMQ_IMAGE must be set via --env-file ../.env.versions}
    command: ["sh", "mqproxy", "-n", "namesrv:9876"]
    environment:
      HEAP_OPTS: "-Xms256m -Xmx256m -Xmn128m"
    ports:
      - "127.0.0.1:8081:8081"
    depends_on:
      broker:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "timeout 3 bash -c '</dev/tcp/127.0.0.1/8081'"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 10s
    # 实验语义要求每次全新启动：不挂持久卷，down 时数据随容器一起销毁。

  # setup 在 rocketmq jar 中为 noop（Topic/消费组由 run.sh 经 mqadmin 创建），声明以保持编排链对称。
  setup:
    <<: *java-base
    depends_on:
      proxy:
        condition: service_healthy
    command: ["java", "-jar", "/app/demo.jar", "setup", "--lab=basic"]

  producer:
    <<: *java-base
    depends_on:
      setup:
        condition: service_completed_successfully
    command:
      - java
      - -jar
      - /app/demo.jar
      - produce
      - --lab=basic
      - --topic=orders-basic
      - --files=order-1001.json,order-1002.json,order-1003.json
      - --fixtures=/app/fixtures
      - --endpoints=proxy:8081

  consumer:
    <<: *java-base
    volumes:
      - ../target/hello-mq-rocketmq.jar:/app/demo.jar:ro
      - ../../shared/fixtures:/app/fixtures:ro
      - idempotency:/data
    depends_on:
      producer:
        condition: service_completed_successfully
    command:
      - java
      - -jar
      - /app/demo.jar
      - consume
      - --topic=orders-basic
      - --group=orders-basic-group
      - --db=/data/idempotency.db
      - --lab=basic
      - --expected=3
      - --endpoints=proxy:8081

  inspect-db:
    <<: *java-base
    volumes:
      - ../target/hello-mq-rocketmq.jar:/app/demo.jar:ro
      - idempotency:/data:ro
    depends_on:
      consumer:
        condition: service_completed_successfully
    command: ["java", "-jar", "/app/demo.jar", "inspect-db", "--lab=basic", "--db=/data/idempotency.db"]

volumes:
  idempotency:
