export type DockerEvidenceStatus='verified'|'partial'|'documented'|'unsupported'
export type DockerRunMode='container'|'custom-image'|'native'|'browser'
export interface DockerImageRef{role:'builder'|'runtime'|'server'|'client';tag:string;digest?:string;digestKey?:string;source:'official'|'vendor'|'custom-official-base'|'native'}
export interface DockerCatalogEntry{id:string;name:string;mode:DockerRunMode;status:DockerEvidenceStatus;images:DockerImageRef[];toolRoots:string[];keyTools:string[];buildCommand:string;runCommand:string;note?:string}
const server=(digestKey:string,tag:string,source:'official'|'vendor'='vendor'):DockerImageRef=>({role:'server',tag,digestKey,source})
export const dockerCatalog:DockerCatalogEntry[]=[
 {id:'kafka',name:'Apache Kafka',mode:'container',status:'verified',images:[server('KAFKA_IMAGE','apache/kafka:4.3.1')],toolRoots:['/opt/kafka/bin'],keyTools:['kafka-topics.sh','kafka-console-producer.sh','kafka-console-consumer.sh','kafka-consumer-groups.sh'],buildCommand:'kafka-topics.sh --create',runCommand:'producer → consumer → consumer-groups --describe'},
 {id:'pulsar',name:'Apache Pulsar',mode:'container',status:'verified',images:[server('PULSAR_IMAGE','apachepulsar/pulsar:4.2.4')],toolRoots:['/pulsar/bin'],keyTools:['pulsar-admin','pulsar-client','pulsar'],buildCommand:'pulsar-admin topics create',runCommand:'pulsar-client produce → consume'},
 {id:'redis-streams',name:'Redis Streams',mode:'container',status:'verified',images:[server('REDIS_IMAGE','redis:8.2.1','official')],toolRoots:['/usr/local/bin'],keyTools:['redis-cli','redis-server','redis-check-aof','redis-check-rdb'],buildCommand:'redis-cli XGROUP CREATE … MKSTREAM',runCommand:'XADD → XREADGROUP → XACK'},
 {id:'rocketmq',name:'Apache RocketMQ',mode:'container',status:'verified',images:[server('ROCKETMQ_IMAGE','apache/rocketmq:5.5.0')],toolRoots:['/home/rocketmq/rocketmq-*/bin'],keyTools:['mqadmin','mqbroker','mqnamesrv'],buildCommand:'mqadmin updateTopic',runCommand:'mqadmin sendMessage → consumeMessage'},
 {id:'artemis',name:'ActiveMQ Artemis',mode:'container',status:'verified',images:[server('ARTEMIS_IMAGE','apache/activemq-artemis:2.44.0')],toolRoots:['/opt/activemq-artemis/bin'],keyTools:['artemis'],buildCommand:'artemis queue create',runCommand:'artemis producer → consumer → browser'},
 {id:'activemq-classic',name:'ActiveMQ Classic',mode:'container',status:'verified',images:[server('ACTIVEMQ_CLASSIC_IMAGE','apache/activemq-classic:6.2.0')],toolRoots:['/opt/apache-activemq/bin'],keyTools:['activemq','activemq-admin'],buildCommand:'管理命令与目标检查',runCommand:'CLI/内置示例完成生产、消费与复查'},
 {id:'rabbitmq',name:'RabbitMQ',mode:'container',status:'partial',images:[server('RABBITMQ_IMAGE','rabbitmq:4.1.4-management','official')],toolRoots:['/usr/local/sbin','/usr/local/bin'],keyTools:['rabbitmqctl','rabbitmq-diagnostics','rabbitmqadmin'],buildCommand:'rabbitmqadmin declare queue',runCommand:'management HTTP API publish/get',note:'镜像自带管理 CLI，但不带独立消息收发 CLI；收发由 management API 辅助。'},
 {id:'nats',name:'NATS',mode:'container',status:'partial',images:[server('NATS_IMAGE','nats:2.11.5','official')],toolRoots:['/'],keyTools:['nats-server'],buildCommand:'无镜像内建队列/主题 CLI',runCommand:'8222 /healthz、/varz、/connz、/subsz',note:'官方镜像为 distroless，仅有 nats-server；nats CLI 独立发行，不伪装成镜像自带能力。'},
]
export const dockerCatalogById=Object.fromEntries(dockerCatalog.map(e=>[e.id,e])) as Record<string,DockerCatalogEntry>
