# hello-mq 本地实验参数说明（不含真实秘密）
# 复制为 .env 后可按需覆盖；.env 已被 .gitignore 排除。
#
# 安全提示：以下账号密码仅用于本地 Docker 实验，严禁用于任何生产环境。

# RabbitMQ 本地演示账号（仅限本地实验，禁止用于生产）
RABBITMQ_DEFAULT_USER=guest
RABBITMQ_DEFAULT_PASS=guest
